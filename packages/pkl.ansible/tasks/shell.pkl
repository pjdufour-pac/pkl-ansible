open module shell

extends "become.pkl"

open class AnsibleTaskDefinitionShell extends AnsibleTaskDefinitionBecome {
    become = true
    become_user = "root"
    shell: String|Shell
    args: Args?
    `when`: When?
    failed_when: When?
}